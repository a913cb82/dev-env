# Skills

- Live — update as lessons emerge (same session).
- Lean — depth in references/; refactor when bloated.
- New skills, extensions, and agent config live in this repo, symlinked from `~/.pi` — run `./sync.sh --check` after creating any.